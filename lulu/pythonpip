python3 苹果系统使用pip自动导入第三方库
sudo python3 -m pip install numpy

接口测试小结：


GET请求：通过URL或者cookie传参，有长度限制，一般用来获取数据
POST请求：数据放在body中，可以非常大，数据在地址栏不可见，相对安全，一般用来发送数据

http状态码：每一个http请求发出都会有一个状态码
200 2开头便是请求发送成功
300 3开头便是重定向，常见302
400 400代表客户端发送的请求有语法错误，401代表访问的页面没有授权
        403代表没有权限访问这个页面，404代表没有这个页面
500 5开头表示服务器有异常，500代表内部异常，504代表服务器端超时，没返回结果。

一、通用接口测试用例设计：
1、通过性验证，按照文档上的参数正常输入，看是否返回正确的结果
2、参数组合：组合传入的参数看返回结果
3、安全性测试：
       绕过验证：提交订单时更该商品价格
       绕过身份授权：普通用户修改商品信息等
       参数是否加密：用户名、密码是否加密及加密强度等
        密码安全规则：密码的复杂度验证
4、异常验证：
       不按照文档上的要求输入参数，如必填的不填等（三种类型：必传非必传，参数类型，入参长度）     
二、根据业务逻辑来设计用例：
 根据业务逻辑来设计，根据需求找出测试点，造数据测试对应的测试点。
